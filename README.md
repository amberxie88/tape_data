# tape_data

Wuji tape datasets, moved out of `aug_data` for standalone backup.
Pushed to GitHub in <2GB waves via `push_dataset_waves.sh` (blobs pruned after each
confirmed wave, so peak extra disk is one wave). Datasets:

- `0824_1740_tape/` (53 trajs)
- `0825_1433_tape/` (151 trajs)
